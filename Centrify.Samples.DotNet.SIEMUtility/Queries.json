{
    "queries": [
        {
            "caption": "Event",
            "query": "Select * from Event where whenoccurred >= DateFunc('now','-30')"
        },
        {
            "caption": "Proxy",
            "query": "Select * from Proxy"
        }
    ]
}