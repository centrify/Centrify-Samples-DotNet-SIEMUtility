{
    "queries": [
        {
            "caption": "Event",
            "query": "select ID AS _ID, NormalizedUser as User, UserGuid as _UserId, applicationid as _ApplicationID, Count(*) as Count from Event where EventType = 'Cloud.Core.Login' and WhenOccurred >= DateFunc('today')"
        },
        {
            "caption": "Proxy",
            "query": "Select * from Proxy"
        }
    ]
}